# Sprint 3 — Milestone Memories

## Tasks

| # | Task | Status | Key files |
|---|------|--------|-----------|
| 1 | Journal tab structure | Done | `lib/ui/journal/journal_screen.dart` (replaces canvas tab) |
| 2 | Journey screen | Done | `journey_tab.dart`, `milestone_repo.dart` — milestone timeline + story viewer |
| 3 | Story screen | Done | `milestone_story_screen.dart` — AI story saved to `milestones/{id}` |
| 4 | Chapter Book | Done | `chapter_book_tab.dart` — unlocks per chapter; export when all 5 unlocked |

## Readiness score (PDF)

`readiness = (mood_weight*40) + (sleep_weight*35) + (stability_weight*25)`

Show Ready Now when score >= 75, state=Calm, sleep >= 8h.

## Never show age comparison language to parents.
